<table class="table table-striped table-bordered table-hover table-condensed">
    {% if path != '' %}
        <tr class="tree back">
            <td colspan="2">
                <i class="icon-arrow-left"></i>
                <a class="tree-object back" href="{{ link_parent() }}">
                    &hellip;
                </a>
            </td>
        </tr>
    {% endif %}
    {% if tree.blob %}
        <tr class="blob" data-commitid="{{ tree.blob.sha }}" data-path="{{ tree.blob.fullPath }}"><td colspan="2">{{ output_content(tree.blob)|raw }}</td></tr>
    {% else %}
        {% for treeObject in tree %}
            <tr class="{{ treeObject.type }}" data-commitid="{{ treeObject.sha }}" data-path="{{ treeObject.fullPath }}">
                <td>
                    {% if 'tree' == treeObject.type %}
                        <i class="icon-folder-close"></i>
                    {% elseif 'blob' == treeObject.type %}
                        <i class="icon-file"></i>
                    {% endif %}
                    <a class="tree-object" href="{{ treeObject|link_tree_object }}">{{ treeObject }}</a>
                </td>
                <td></td>
            </tr>
        {% endfor %}
    {% endif %}
</table>