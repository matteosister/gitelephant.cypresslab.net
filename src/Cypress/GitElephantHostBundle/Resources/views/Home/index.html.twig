{% extends "CypressGitElephantHostBundle::base.html.twig" %}

{% block body %}
    <h1>Git Repositories</h1>
    <ul>
        {% for repository in repositories %}
            <li><a href="{{ path('repository', { slug: repository.slug }) }}">{{ repository.git.path }}</a></li>
        {% endfor %}
    </ul>
{% endblock %}
