<table class="table table-striped table-bordered table-hover table-condensed">
    {% if path != '' %}
        <tr class="tree">
            <td colspan="2">
                <i class="icon-arrow-left"></i>
                <a class="tree-object back" href="{{ link_parent() }}">
                    &hellip;
                </a>
            </td>
        </tr>
    {% endif %}
    {% if tree.blob %}
        <tr class="blob"><td colspan="2">{{ output_content(tree.blob) }}</td></tr>
    {% else %}
        {% for treeObject in tree %}
            <tr class="{{ treeObject.type }}" id="{{ treeObject.sha }}">
                <td>
                    {% if 'tree' == treeObject.type %}
                        <i class="icon-folder-close"></i>
                    {% elseif 'blob' == treeObject.type %}
                        <i class="icon-file"></i>
                    {% endif %}
                    <a class="tree-object" href="{{ treeObject|link_tree_object }}">{{ treeObject }}</a>
                </td>
                <td></td>
            </tr>
        {% endfor %}
    {% endif %}
</table>